  <!-- Sidebar -->
@{ 
    var controller = ViewContext.RouteData.Values["Controller"];
    var action = ViewContext.RouteData.Values["Action"];
    var activeDashboard = controller.ToString().ToLower().Equals("dashboard") ? "active" : "";
    var activePedidos = controller.ToString().ToLower().Equals("pedidos") ? "active" : "";
}

<ul class="sidebar navbar-nav">
    <li class="nav-item @activeDashboard">
        <a class="nav-link" asp-controller="Dashboard" asp-action="Index">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>
    <li class="nav-item @activePedidos">
        <a class="nav-link" asp-controller="Pedidos" asp-action="Index">
            <i class="fas fa-fw fa-truck-moving"></i>
            <span>Pedidos</span>
        </a>
    </li>
</ul>
